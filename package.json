{
  "name": "on-paint",
  "author": {
    "name": "Nathan Wright"
  },
  "version": "0.5.4",
  "homepage": "https://github.com/nullset/on-paint",
  "description": "A small library to run any number of actions on every animation frame, meaning these actions will run as fast as your web browser is capable of running them.",
  "keywords": [
    "requestAnimationFrame",
    "onpaint",
    "on-paint"
  ],
  "browser": "src/index.js",
  "main": "src/index.js",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^2.17.1"
  },
  "dependencies": {}
}
